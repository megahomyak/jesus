# Turns every function or class you want into Wine.

* Import `water` from `jesus`.

* Wrap class or function with `@water` to turn it into Wine.
  Wine can do nothing, but it is Wine, so that's great!

# Available on pip!

    pip install jesus
